# URL of the Express backend (used by Next.js API routes to proxy requests)
EXPRESS_URL=http://localhost:3000

# Public base URL of the backend API (used by browser-side axios in lib/api.ts)
NEXT_PUBLIC_API_URL=http://localhost:3000
